<div class="new-schedules">
  <div class="new-schedule">
    <div class="daily-schedule-title">
      <%= "#{@care_user.name}さん　過去一ヶ月のケア一覧" %>
    </div>
    <%= render partial: 'past_content', collection: @past_schedules, as: "schedule" %>
    <div class="back-past">
      <div class="back">
        <%= link_to "#{@care_user.name}さんの一覧へ戻る", care_user_schedules_path %>
      </div>
    </div>
  </div>
</div>